# ****************** MAMP MySQL DB CONFIG ******************
salary.db.driver=org.h2.Driver
salary.db.url=jdbc:h2:mem:salary_management;MODE=MySQL;DB_CLOSE_ON_EXIT=TRUE;DB_CLOSE_DELAY=-1;
salary.db.username=root
salary.db.password=root

#Hibernate
hibernate.dialect=org.hibernate.dialect.H2Dialect
#hibernate.hbm2ddl.auto=validate
hibernate.show_sql=false
hibernate.batch.size=20
